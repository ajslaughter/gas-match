# Price source mode: "estimated" (default, no key) or "live" (stubbed — plug in your adapter)
PRICE_MODE=estimated

# Contact email for Nominatim/Overpass User-Agent header (polite usage policy).
# Both services are free but require a real identifier. Use your project email.
CONTACT_EMAIL=dev@example.com
